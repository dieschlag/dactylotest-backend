model User {
  id                Int       @id @default(autoincrement())
  username			String?
  first_name        String
  last_name         String
  email             String    @unique
  password          String
  created_at        DateTime  @default(now())
  created_by        Int?
  updated_at        DateTime  @default(now())
  updated_by        Int?
  deleted_at        DateTime?
  deleted_by        Int?
}